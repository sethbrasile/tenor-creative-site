---
phase: 01-foundation-strategy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, app/app.vue, app/pages/privacy.vue, app/pages/terms.vue, app/pages/privacy-policy.vue, app/pages/terms-of-use.vue]
autonomous: true

must_haves:
  truths:
    - "Site loads without legacy HubSpot scripts in the source code"
    - "User can navigate to /privacy and /terms and see consolidated legal content"
    - "Legal pages list Oklahoma as the governing jurisdiction"
  artifacts:
    - path: "app/pages/privacy.vue"
      provides: "Consolidated privacy policy"
    - path: "app/pages/terms.vue"
      provides: "Consolidated terms of use with SMS section"
  key_links:
    - from: "app/pages/privacy.vue"
      to: "app/pages/terms.vue"
      via: "internal links"
---

<objective>
Remediate technical debt and consolidate legal documentation to establish a clean foundation for the redesign.

Purpose: Remove legacy HubSpot dependencies and simplify the legal page structure to improve maintainability and SEO.
Output: Cleaned codebase, installed Zod, and consolidated legal pages.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-strategy/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Technical Debt Remediation</name>
  <files>package.json, app/app.vue</files>
  <action>
    1. Remove `@hubspot/api-client` from dependencies using `pnpm remove`.
    2. Install `zod` dependency using `pnpm install`.
    3. Clean up `app/app.vue`:
       - Remove the commented out HubSpot script block (lines 8-12).
       - Ensure third-party scripts are correctly handled via `useHead`.
  </action>
  <verify>
    - `grep "@hubspot/api-client" package.json` returns nothing.
    - `grep "zod" package.json` shows the package installed.
    - `grep "hs-scripts" app/app.vue` returns nothing.
    - `npm run build` passes.
  </verify>
  <done>Legacy HubSpot code is removed and Zod is ready for use.</done>
</task>

<task type="auto">
  <name>Task 2: Legal Page Consolidation</name>
  <files>app/pages/privacy.vue, app/pages/terms.vue, app/pages/privacy-policy.vue, app/pages/terms-of-use.vue</files>
  <action>
    1. Update `app/pages/privacy.vue`:
       - Replace content with the more detailed content from `app/pages/privacy-policy.vue`.
       - Ensure location is "Durant, OK" or "Oklahoma".
    2. Update `app/pages/terms.vue`:
       - Replace content with the more detailed content from `app/pages/terms-of-use.vue` (ensure SMS Program Terms are included).
       - Change Governing Law from Delaware to Oklahoma (Section 11).
       - Ensure location is "Durant, OK" or "Oklahoma" (Section 13).
    3. Delete `app/pages/privacy-policy.vue` and `app/pages/terms-of-use.vue`.
    4. Update internal links in legal pages to point to `/privacy` and `/terms` instead of the longer paths.
  </action>
  <verify>
    - `/privacy` and `/terms` pages exist and contain correct content.
    - Old paths return 404 (or are removed from codebase).
    - No mention of "Delaware" remains in legal pages.
  </verify>
  <done>Legal pages are consolidated and reflect the Oklahoma jurisdiction.</done>
</task>

</tasks>

<verification>
- Verify HubSpot scripts are gone from browser source.
- Verify /privacy and /terms load correctly with consolidated content.
</verification>

<success_criteria>
- Technical debt remediated (no hubspot, zod present).
- Legal pages consolidated and accurate to Oklahoma branding.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-strategy/01-01-SUMMARY.md`
</output>
