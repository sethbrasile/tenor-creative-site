---
phase: 01-foundation-strategy
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [nuxt.config.ts, server/utils/ghl.ts, server/api/contact.post.ts]
autonomous: true
user_setup:
  - service: GoHighLevel (GHL)
    why: "CRM integration for contact forms"
    env_vars:
      - name: GHL_API_KEY
        source: "GHL Marketplace -> API Keys (Version 2)"

must_haves:
  truths:
    - "GHL API client is successfully authenticated"
    - "Contact form submissions are validated using Zod schemas"
  artifacts:
    - path: "server/utils/ghl.ts"
      provides: "Lightweight ofetch wrapper for GHL API v2"
    - path: "server/api/contact.post.ts"
      provides: "Zod-validated contact submission handler"
  key_links:
    - from: "server/api/contact.post.ts"
      to: "server/utils/ghl.ts"
      via: "ghlFetch utility"
---

<objective>
Establish GoHighLevel (GHL) connectivity and implement robust server-side validation for lead capture.

Purpose: Replace HubSpot lead capture with GHL and ensure data integrity via Zod validation.
Output: Configured GHL utility and updated contact API endpoint.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-strategy/01-RESEARCH.md
@.planning/phases/01-foundation-strategy/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: GHL Configuration & Utility</name>
  <files>nuxt.config.ts, server/utils/ghl.ts</files>
  <action>
    1. Add `ghlApiKey` to `runtimeConfig` in `nuxt.config.ts`.
    2. Create `server/utils/ghl.ts`:
       - Export a `ghlFetch` instance using `$fetch.create`.
       - Base URL: `https://services.cloud.msgndr.com/generic` (Verify against research if possible, or use standard GHL v2 base).
       - Include `Authorization: Bearer ${useRuntimeConfig().ghlApiKey}` and `Version: 2021-04-15` headers.
  </action>
  <verify>
    - `server/utils/ghl.ts` exists and exports `ghlFetch`.
    - `nuxt.config.ts` includes `ghlApiKey` in runtimeConfig.
  </verify>
  <done>GHL utility is ready for server-side use.</done>
</task>

<task type="auto">
  <name>Task 2: Validated Contact Handler</name>
  <files>server/api/contact.post.ts</files>
  <action>
    1. Update `server/api/contact.post.ts`:
       - Import `z` from `zod`.
       - Define `contactSchema` (firstname, email, phone, message).
       - Use `readValidatedBody` to validate the request.
       - Use `ghlFetch` to post the contact to GHL (Create/Update Contact endpoint).
       - Map the body fields to GHL's expected payload.
       - Implement proper error handling for 4xx/5xx responses from GHL.
  </action>
  <verify>
    - `curl -X POST -d '{"firstname":"Test", "email":"test@example.com", "message":"Hello"}' /api/contact` returns 200 (if API key provided) or handles error gracefully.
    - Invalid payloads return 400 Bad Request.
  </verify>
  <done>Contact API is upgraded with Zod validation and GHL integration.</done>
</task>

</tasks>

<verification>
- Test contact submission with valid and invalid data.
- Check GHL (if access available) or verify the API request payload in logs.
</verification>

<success_criteria>
- GHL utility configured and used by contact API.
- All contact submissions are validated via Zod.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-strategy/01-02-SUMMARY.md`
</output>
